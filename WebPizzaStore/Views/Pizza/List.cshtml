@model WebPizzaStore.Models.Pizza

<h1>Pizza Details</h1>

<div>
    <h4>Piza ID: @Model.Id</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Name:</dt>
        <dd class="col-sm-10">@Model.Name</dd>
        <dt class="col-sm-2">Price:</dt>
        <dd class="col-sm-10">@Model.Price</dd>
        <dt class="col-sm-2">Size:</dt>
        <dd class="col-sm-10">@Model.Size</dd>
        <dt class="col-sm-2">IS Guluten Free:</dt>
        <dd class="col-sm-10">@Model.IsGlutenFree</dd>
        <dt class="col-sm-2">Sauce:</dt>
        <dd class="col-sm-10">@Model.Sauce</dd>
    </dl>
</div>

<p>
    <a asp-action="Index">Back to List</a>
</p>
